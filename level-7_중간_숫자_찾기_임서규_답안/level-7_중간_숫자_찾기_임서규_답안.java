public static int getNumber(int[] input) {
    int isError = -1; //조건 불충족시 -1반환
	Arrays.sort(input); //오름차순 정렬
	if(input.length > 3){
	    return isFalse;
	}else{
	    return input[1];
	}
 }
